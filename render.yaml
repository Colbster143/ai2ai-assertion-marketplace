services:
  - type: web
    name: ai2ai-marketplace
    env: node
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: PORT
        value: "10000"
      - key: NODE_VERSION
        value: "22.0.0"
